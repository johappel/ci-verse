# CI-Verse Production Environment Configuration (GitHub Pages)

# ============================================================================
# MOCK DATA MODE (für GitHub Pages deployment)
# ============================================================================
# Da GitHub Pages ein statischer Host ohne Backend ist, verwenden wir Mock-Daten
VITE_USE_MOCK_DATA=true

# WordPress URL wird ignoriert wenn VITE_USE_MOCK_DATA=true
# Aber wird als Default gesetzt für Fallback-Szenarien
VITE_WP_URL=http://ci.test
